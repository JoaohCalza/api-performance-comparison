server {
    listen 80;

    # ======================================
    # API 1 - Laravel (ATIVA)
    # ======================================

    # Rota principal -> envia tudo para API Laravel
    root /var/www/html/laravel/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass laravel1:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    # ======================================
    # API 2 - NodeJS (COMENTADA)
    # Caminho sugerido: /api2
    # ======================================

    # location /api2/ {
    #     proxy_pass http://api2:3000/;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_cache_bypass $http_upgrade;
    # }

    # ======================================
    # API 3 - Python FastAPI (COMENTADA)
    # Caminho sugerido: /api3
    # ======================================

    # location /api3/ {
    #     proxy_pass http://api3:5000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # }
}
